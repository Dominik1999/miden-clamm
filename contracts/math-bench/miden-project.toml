[package]
name = "math-bench"
version = "0.1.0"

[lib]
kind = "account-component"
# Full `miden:<package>/<interface>@<version>` id. The interface segment is the
# kebab-cased component trait name (`MathBench` -> `math-bench`).
namespace = "miden:math-bench/math-bench@0.1.0"

[dependencies]
miden-core = "*"
miden-protocol = "*"

[package.metadata.miden]
supported-types = ["RegularAccountImmutableCode"]
